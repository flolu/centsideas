apiVersion: apps/v1
kind: Deployment
metadata:
  name: user-read
spec:
  selector:
    matchLabels:
      app: user-read
  replicas: 2
  template:
    metadata:
      labels:
        app: user-read
    spec:
      containers:
        - name: user-read
          image: user-read:placeholder_name
          ports:
            - containerPort: 3000
          readinessProbe:
            httpGet:
              path: /
              port: 3000
            initialDelaySeconds: 5
            periodSeconds: 3
          livenessProbe:
            httpGet:
              path: /
              port: 3000
            initialDelaySeconds: 5
            periodSeconds: 10
          envFrom:
            - configMapRef:
                name: config
          resources:
            requests:
              memory: 50Mi
              cpu: 15m
            limits:
              memory: 300Mi
              cpu: 200m
          env:
            - name: secrets.readDatabase.password
              valueFrom:
                secretKeyRef:
                  name: secrets
                  key: secrets.readDatabase.password
      affinity:
        nodeAffinity:
          preferredDuringSchedulingIgnoredDuringExecution:
            - preference:
                matchExpressions:
                  - key: cloud.google.com/gke-preemptible
                    operator: Exists
              weight: 100

---
apiVersion: v1
kind: Service
metadata:
  name: user-read
spec:
  selector:
    app: user-read
  ports:
    - name: http
      port: 3000
  type: ClusterIP

---
apiVersion: autoscaling/v1
kind: HorizontalPodAutoscaler
metadata:
  name: user-read
spec:
  scaleTargetRef:
    apiVersion: apps/v1beta1
    kind: Deployment
    name: user-read
  minReplicas: 2
  maxReplicas: 5
  targetCPUUtilizationPercentage: 75
