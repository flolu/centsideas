export enum IdeaTitleLength {
  Max = 100,
  Min = 3,
}

export enum IdeaTagsCount {
  Max = 25,
}

export enum IdeaTagsLength {
  Max = 30,
  Min = 2,
}

export enum IdeaDescriptionLength {
  Max = 3000,
}
