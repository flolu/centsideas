export enum ReviewsApiRoutes {
  SaveDraft = 'save-draft',
  Publish = 'publish',
}

export enum ReviewsApiInternalRoutes {
  Create = 'create',
  SaveDraft = 'save-draft',
  Publish = 'publish',
  Update = 'update',
  Unpublish = 'unpublish',
}
