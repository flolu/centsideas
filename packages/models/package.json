{
  "name": "@cents-ideas/models",
  "version": "0.0.0",
  "main": "dist/index.js",
  "license": "MIT",
  "private": true,
  "scripts": {
    "clean": "rimraf dist && rimraf coverage && rimraf node_modules",
    "build": "tsc"
  }
}
